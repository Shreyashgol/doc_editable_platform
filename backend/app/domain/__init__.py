"""Domain layer — pure business model.

This package depends on the Python standard library only. It must not import FastAPI,
SQLAlchemy, Celery, Pydantic, or any infrastructure. Everything here is unit-testable in
isolation, which is what makes the >=90% coverage target reachable.
"""
